{
  "name": "shiny-websocket-relay",
  "version": "0.0.1",
  "description": "A websocket server allowing to transfer push messages from a client to another client.",
  "dependencies": {
    "socket.io": "^4.0.1"
  }
}
